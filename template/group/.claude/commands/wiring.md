---
allowed-tools: [Read, Write, Edit, Bash]
description: Manage vault wiring — register repos, write CLAUDE.md/AGENTS.md pointers, sync agent context
argument-hint: <add <path> | remove <name> | status | sync>
# vault-local: this command must only exist in <vault>/.claude/commands/ — never in ~/.claude/commands/
---

@.agents/commands/wiring.md
