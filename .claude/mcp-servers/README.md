# MCP Servers

The vault MCP implementation moved to the Python `shmulistan-template` repo: `https://github.com/shmul95/shmulistan-template`. This repository now consumes that server via `.mcp.json`; local legacy JS vault MCP tooling has been retired as part of Phase 1 cutover.
